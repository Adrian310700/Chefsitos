meta {
  name: (m)crear_categoria copy
  type: http
  seq: 4
}

post {
  url: http://localhost:8080/api/v1/categorias
  body: json
  auth: inherit
}

body:json {
  {
    "nombreCategoria": "",
    "descripcion": "Esto debería fallar por el @NotBlank"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
